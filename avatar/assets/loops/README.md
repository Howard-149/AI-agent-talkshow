# Pre-baked idle motion loops (MP4)

**Canonical path:** [`avatar/assets/loops/`](../../../../avatar/assets/loops/)

`talkshow-web/public/avatars/loops` is a symlink to that directory. Bake with:

```bash
bash deploy/bake-avatar-assets.sh
```

Expected: `lessac-idle.mp4`, `ryan-idle.mp4`, `amy-idle.mp4`
