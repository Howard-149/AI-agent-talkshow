# Babel GPU node — conda env `talkshow`:
#   pip install -r requirements.txt
#
# 本機筆電請用 requirements-laptop.txt
#
# vLLM 必須 +cu129 wheel（勿 plain `pip install vllm`）。見 docs/CLUSTER_PROBE.md

--extra-index-url https://download.pytorch.org/whl/cu129

# LiveKit agent (pip 只裝 Python 套件；ONNX 權重另跑 deploy/download-livekit-agent-models.sh)
livekit-api>=0.8.0
livekit-agents>=1.2.0
livekit-plugins-silero>=1.0.0
livekit-plugins-turn-detector>=1.0.0

# App
huggingface_hub>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
numpy>=1.26.0

# TTS
piper-tts>=1.2.0

# vLLM + Gemma audio-in (CUDA 12.9 / Babel L40S)
torch==2.11.0
https://github.com/vllm-project/vllm/releases/download/v0.21.0/vllm-0.21.0+cu129-cp38-abi3-manylinux_2_34_x86_64.whl
av>=12.0.0
scipy>=1.11.0
soundfile>=0.12.0
