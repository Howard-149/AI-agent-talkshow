# Babel GPU node — conda env `talkshow` (Python 3.11):
#   conda create -n talkshow python=3.11 -y
#   conda activate talkshow
#   pip install -r requirements.txt
#
# Laptop: use requirements-laptop.txt instead
#
# vLLM needs +cu129 wheel (not plain `pip install vllm`). See docs/CLUSTER_PROBE.md if present.

--extra-index-url https://download.pytorch.org/whl/cu129

# LiveKit agent (pip installs Python packages only; run deploy/download-livekit-agent-models.sh for ONNX weights)
livekit-api>=0.8.0
livekit-agents>=1.2.0
livekit-plugins-silero>=1.0.0
livekit-plugins-turn-detector>=1.0.0

# App
huggingface_hub>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
numpy>=1.26.0

# TTS — Piper ONNX on GPU (CUDA 12.x). If pip also installs cpu `onnxruntime`, remove it:
#   pip uninstall -y onnxruntime
# [zh] pulls g2pw (+ friends) for Chinese voices (huayan / xiao_ya / chaowen).
piper-tts[zh]>=1.2.0
onnxruntime-gpu[cuda,cudnn]>=1.20.0

# vLLM + Gemma audio-in (CUDA 12.9 / Babel L40S)
torch==2.11.0
https://github.com/vllm-project/vllm/releases/download/v0.21.0/vllm-0.21.0+cu129-cp38-abi3-manylinux_2_34_x86_64.whl
av>=12.0.0
scipy>=1.11.0
soundfile>=0.12.0
